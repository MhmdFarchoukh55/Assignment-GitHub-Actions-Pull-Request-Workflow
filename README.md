
first we switch ti the branch feature/add-workflows by using this command 'git switch feature/add-workflows'
second we create a folder github then using this command mkdir ./github then we create another folder that named workflows
third we go to this path ./github/workflows
4- we create a yml file by using this command New-Item manual-trigger.yml
5-we want to open it by using this command code manual-trigger.yml
6-we write a manual trigger on the yml file
7-we push the code on the branch that we created
8-we create a second yml file that name merge-to-main.yml
9-we open the merge-to-main.yml we put a message 
10- we merge the branch that we crrated to the main branch or we call it master branch
